#!/bin/bash

function task_build()
{
    if [ ! -f /app/package.json ]; then
        task skeleton:apply
    fi

    task overlay:apply

    {% for step in @('app.gateway.build.steps') -%}
    {{ step|raw }}
    {% endfor %}

}
