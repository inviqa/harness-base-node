#!/bin/bash

function task_build_gateway()
{
    cd {{ @('gateway.path') }}

    if [ ! {{ @('gateway.build.when')|raw }} ]; then
        return 0;
    fi
    
    {% for step in @('gateway.build.steps') -%}
    {{ step|raw }}
    {% endfor %}

}
