#!/bin/bash

function task_build_client()
{
    cd {{ @('client.path') }}

    if [ ! {{ @('client.build.when')|raw }} ]; then
        return 0;
    fi

    {% for step in @('client.build.steps') -%}
    {{ step|raw }}
    {% endfor %}

}
